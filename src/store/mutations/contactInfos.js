let contactInfosMutations = {
 //mutations for contactinfos there
 //most likely will not need to mutate since the contact list is updated with contact infos
}

module.exports = contactInfosMutations;
